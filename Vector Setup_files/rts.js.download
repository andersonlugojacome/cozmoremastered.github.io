// Function to check and log the online/offline status
function updateOnlineStatus() {
  if (navigator.onLine) {
    console.log("online");
  } else {
    console.log("offline");
  }
}

// Add event listeners for online and offline events
window.addEventListener('online', updateOnlineStatus);
window.addEventListener('offline', updateOnlineStatus);

// Initial check
updateOnlineStatus();

// Your existing code to handle events
$(document).ready(function () {
  $(".vec-env-select-btn").click(function () {
    var selectedStack = $("#envOptions").val();
    handleStackSelection(selectedStack);
  });

  $("#btnDiscoverVector").click(function () {
    console.log("hbbi");
  });
});
