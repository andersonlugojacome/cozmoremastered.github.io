$(".vec-env-select-btn").click(function () {
  // Hide the current page (containerEnvironment)
  $("#containerEnvironment").hide();

  // Show the next page (containerDiscover)
  $("#containerDiscover").show();
});
